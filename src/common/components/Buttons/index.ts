export * from './variant';
export * from './types';

import { BackButton, CancelButton, LinkButton, SaveButton } from './variant';

export const Buttons = {
  Back: BackButton,
  Cancel: CancelButton,
  Link: LinkButton,
  Save: SaveButton,
};
